<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>
    <%= title %>
  </title>
</head>

<body>
  <div class="wrapper">
    <%- include('partials/header') %>

      <div class="container">
        <% if (user) { %>
          <% if (title !=='Admin' && title !=='Section' && title !=='Endpoints' ) { %>
            <%- include('partials/sidebar') %>
              <% } %>
                <% } %>


                  <div id="content">
                    <%- body %>
                  </div>
      </div>
  </div>

  <%- include('partials/scripts') %>
</body>

</html>