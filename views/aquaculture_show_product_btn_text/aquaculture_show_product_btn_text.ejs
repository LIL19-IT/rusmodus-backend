<div class="row">
    <h2 class="page-title">
        <%= title %>
    </h2>

    <div class="btn-wrapper">
        <a href="/admin/aquaculture_show_product_btn_text/create" class="btn-create">Create New Product Btn text<%= title %></a>
    </div>

    <table class="styled-table">
        <thead>
            <tr>
                <th>N</th>
                <th>Lang</th>
                <th>Text</th>
                <th colspan="2">Actions</th>
            </tr>
        </thead>

        <tbody>
            <% btnTexts.forEach((elem, index)=> { %>
                <tr>
                    <td>
                        <%= index + 1 %>
                    </td>
                       <td>
                        <%= elem.lang %>
                    </td>
                    <td>
                        <%= elem.text %>
                    </td>

                    <td>
                        <button class="btn btn-edit"
                            onclick="location.href='/admin/aquaculture_show_product_btn_text/edit/<%= elem.id %>'">Edit</button>

                        <form action="/admin/aquaculture_show_product_btn_text/remove/<%= elem.id %>" method="POST"
                            style="display:inline;">
                            <button type="submit" onclick="return confirm('Are you sure you want to delete this item?')"
                                class="btn btn-remove">
                                Remove
                            </button>
                        </form>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
</div>